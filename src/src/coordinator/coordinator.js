export const vaiParaHome = (navigate) => {
    navigate("/")
}

export const vaiParaDetalhes = (navigate, name) => {
    navigate(`/detalhes/${name}`)
}

export const vaiParaPokedex = (navigate) => {
    navigate("/pokedex")
}